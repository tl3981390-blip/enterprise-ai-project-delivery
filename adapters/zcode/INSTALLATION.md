# ZCode 安装映射
技能目录：`~/.zcode/skills/enterprise-ai-project-delivery/`（现有安装指向 canonical core）。
调用：会话内 Skill 工具显式加载（`/enterprise-ai-project-delivery` 或任务类型触发）。
遥测：core recorder 直调，log/anchor 写项目工作区 telemetry/。
