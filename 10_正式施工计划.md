# 10｜正式施工计划（设计与分期）

> 目的：把「企业AI项目交付 Skill」从设计推进到可执行实现的分阶段施工计划。**本文件仅规划；执行在以后阶段由你授权后开始。**
> 每个阶段含：目标 / 输入 / 工作范围 / 禁止范围 / 完成条件 / 真实验收方式 / 失败处理 / 下一阶段门禁。

---

## 0. 总体约定

- **语言**：目录、SKILL.md、references 中文为主（你与我方阅读人群）；frontmatter 的 `name/description` 依开放标准建议保留英文/双语可触发，正文中文。
- **标准**：所有 SKILL.md 遵循 agentskills-standard frontmatter 与渐进式披露（`02/04`）。
- **架构**：方案 B（主 Skill + 子 Skill/模块），`skill_id=enterprise-ai-project-delivery`，semver（`01/08`）。
- **来源**：每个复制经许可（MIT/Apache）的上游文件头部加声明，OpenAI `.system` 全程禁止（`09`）。
- **证据**：每阶段产生 `tests/evals/` 与 `05_测试与验收证据/`（`06/07`）。

---

## 施工阶段总览

| 阶段 | 名称 | 产出 |
| ---- | ---- | ---- |
| 1 | 建立 Skill 骨架 | 主 SKILL + 目录 + 共享件 + 版本/license 脚手架 |
| 2 | 项目理解与需求模块 | 01/02/03 |
| 3 | SDD/TDD 模块 | 04/05/06 |
| 4 | RAG/Agent/权限/治理 | 07/08/09/10 |
| 5 | 施工与失败恢复 | 11/12 |
| 6 | 验收与 Evidence | 13/14/15 |
| 7 | 部署 / License / 升级回滚 | 16/17/18 |
| 8 | 真实案例测试 | 全模块哑项目跑通 + Regression |
| 9 | Harness 适配 | `04_Harness接入适配`（Skill 侧合同落地） |
| 10 | Release | LICENSE/NOTICE、CHANGELOG、最终验收、入库 `03` |

> 阶段 9 需要动 Harness/Adapter；本 Skill 施工在其自身目录完成，对 Harness 主工程的改动以另行授权为准。

---

## 阶段 1：建立 Skill 骨架（含理解门禁骨架）

- **目标**：主 SKILL + 目录树 + 共享 references/scripts + Schema + LICENSE/NOTICE/CHANGELOG/README；**并落地施工前理解门禁完整骨架**（UNDERSTANDING→合同→Gate→READY_TO_PLAN，不得留 TODO）。
- **输入**：`01`—`09` 设计文档（含本次理解门禁补强后的 `01/04/05/06`）。
- **工作范围**：
  - 创建 `企业AI项目交付Skill/` 下完整目录（`04` 模块树，含 `00_总控/references/`、`共享/schema/`、`tests/evals/`）。
  - 主 `SKILL.md`（frontmatter、触发、S0 理解门禁、编排、门禁、输出合同）。
  - `00_总控/SKILL.md`：UNDERSTANDING → 任务理解合同 → 施工前理解门禁 → PLAN → 计划-合同对账 → READY_TO_EXECUTE 完整骨架（非 TODO）。
  - `00_总控/references/`：任务理解合同模板、施工前理解门禁、目标漂移检查清单、状态与权限矩阵。
  - 共享 `references/Definition-of-Done.md`、`references/公共检查清单.md`。
  - 共享 `scripts/`：`validate-skill`、`check_understanding_gate`、`check_plan_alignment`、`collect_evidence`（stdout 结构化、`--help`、破坏性 `--dry-run`）。
  - 共享 `schema/`：`task_understanding_contract.schema.json`、`output_schema.json`、`evidence_schema.json`、`input_schema.json`。
  - `LICENSE`、`NOTICE`、`CHANGELOG.md`、根 `README.md`；初始化 Git 并做首次提交。
- **禁止范围**：不实现任何业务模块逻辑；不开编码项目功能；不连接生产系统；不修改 Harness。
- **完成条件**：骨架目录存在、主 SKILL/S0 可被 `validate-skill` 结构校验通过；合同/输入输出/evidence schema 文件就位且可被 JSON Schema 校验；**理解门禁骨架可演出一条完整合法状态序列（UNDERSTANDING→…→COMPLETED）**；Git 可核验。
- **真实验收**：跑 `validate-skill` 结构/引用/版本/license 检查，无错误；跑 schema 校验器对示例合同/输出 JSON 校验通过；跑**状态机合法跳转测试**（`tests/evals/state_machine/`）0 错误。
- **失败处理**：校验报错 → 修复 → 重跑，至 0 错误；无法自动修则记录并暂停。
- **门禁**：结构 eval + schema 校验 + 状态机跳转测试全过（0 ERROR，对应 `25_阶段1真实验收`）→ 可进阶段 2。

## 阶段 2：项目理解与需求模块（01/02/03）
- **目标**：实现目标五问、存量审计、需求与边界三模块工作流。
- **工作范围**：三个模块的 SKILL.md（Overview/When to Use/Core Process/反合理化/Red Flags/Verification）+ references 模板 + scripts（audit_scan、check_requirements 禁用词检查）。
- **禁止范围**：不进入编码阶段逻辑；模块 01-03 不触碰权限/RAG。
- **完成条件**：三模块结构过校验；触发描述过 should/should-not 触发测试。
- **真实验收**：行为 eval（哑项目：无目标→必须 blocked；含禁用词→退回）；**理解门禁专项 Eval A/E 通过（对应 `07` 2.5）**。
- **失败处理**：断言失败→定位→修复→重跑 eval；≥N 轮仍败→人工介入。
- **门禁**：三模块 behavioral 用例 PASS → 下阶段。

## 阶段 3：SDD/TDD 模块（04/05/06）
- **目标**：规格先行 + 测试策略 + 架构设计模块。
- **工作范围**：04 SDD 模板与机械可检查条目、05 TDD 适用性判断（核心逻辑必测）、06 架构评审清单。
- **禁止范围**：不施工 RAG/Agent/MCP。
- **完成条件**：模块 `Verification` 可机检；TDD 判断输入边界输出可判。
- **真实验收**：行为 eval（规格缺项→blocked；TDD 误判→修正用例）。
- **门禁**：模块 04-06 eval 过 → 下阶段。

## 阶段 4：治理设计模块（07/08/09/10）
- **目标**：RAG 四防、Agent 角色分离、权限网关、企业治理。
- **工作范围**：07 RAG 方案 + verify_citation（防假引用）、08 角色职责分离、09 权限矩阵 + check_permission（READ/WRITE/DELETE/EXECUTE/ADMIN/EXTERNAL）、10 治理清单。
- **禁止范围**：不实际接任何企业系统/MCP。
- **完成条件**：权限矩阵 schema 过校验；四防用例设计就绪。
- **真实验收**：专项 eval（越权→拒；旧版→拒；假引用→抓出；缺权限→blocked）。
- **门禁**：专项 eval 过 → 下阶段。

## 阶段 5：施工与失败恢复（11/12）
- **目标**：增量实现流程 + 失败处理/停止条件。
- **工作范围**：11 施工管理（DoD 挂钩、每增量测试+提交）、12 失败处理（根因+证据保留+自动/人工修复）+ stop_condition（max_loop/human_required）。
- **禁止范围**：不批量一次性大段施工；不无限重试。
- **完成条件**：增量实现流程 + enable 循环收敛验证。
- **真实验收**：行为 eval——注入失败时 status=blocked 且留证据，不假 PASS；≥max_loop 自动暂停交人工。
- **门禁**：循环收敛 + 失败路径 eval 过 → 下阶段。

## 阶段 6：验收与 Evidence（13/14/15）
- **目标**：浏览器真实验收、四角色验收、Evidence 与防假验收。
- **工作范围**：13 浏览器检查、14 四角色模板、15 evidence bundle + collect_evidence + 假验收识别。
- **禁止范围**：不把「代码存在」当完成。
- **完成条件**：evidence bundle 可打、schema 校验过、四角色独立证据可生成。
- **真实验收**：行为 eval——缺证据→blocked；模型文字当证据→拒绝；四角色缺签→不能进下阶段。
- **门禁**：evidence 与防假 eval 过 → 下阶段。

## 阶段 7：部署 / License / 升级回滚（16/17/18）
- **目标**：部署完备、License 扫描、semver 与回滚。
- **工作范围**：16 部署清单、17 scan_license（代码/依赖/模型/数据）、18 semver_check + 回滚演练模板。
- **完成条件**：license 扫描输出分色；semver 与 staging/adopt 流程就绪。
- **真实验收**：专项 eval——红色许可→阻断；回滚演练真实执行能恢复。
- **门禁**：模块 16-18 eval 过 → 下阶段。

## 阶段 8：真实案例测试（全模块）
- **目标**：用真实企业 AI 场景哑项目跑通全模块，Regression。
- **工作范围**：建立案例库（如"内部 RAG 法务问答"）；端到端走 01→19；记录 grading/benchmark。
- **完成条件**：任一关键案例全链 PASS，证据完整；pass_rate 相对基线不下降。
- **真实验收**：三阶段 eval + 端到端案例 + `05` 证据落库。
- **失败处理**：案例失败→回到对应模块修复→回归重跑。
- **门禁**：案例全跑通 + Regression 通过 → 下阶段。

## 阶段 9：Harness 适配（Skill 侧合同落地）
- **目标**：把 `05` 合同落到可交付物，供 `04_Harness接入适配` 使用。
- **工作范围**：补充 Harness 声明文件（required/allowed tools、mcp、permissions schema）、entrypoint 约定、discover/invoke 兼容层说明。
- **禁止范围**：**不得修改 Harness 主工程**；对 Harness 改动需另行授权并走 `04`。
- **完成条件**：合同 JSON Schema 可被一个 mock Harness 校验通过。
- **真实验收**：mock 校验器跑通 discover/invoke/advance/suspend/resume。
- **门禁**：mock 全过 → 可进入 Release。

## 阶段 10：Release
- **目标**：正式签发，形成第一版稳定 Skill。
- **工作范围**：签发 `LICENSE`/`NOTICE`、写 `CHANGELOG`、semver 定版（`1.0.0`）、最终四角色验收、打包内容校验、同步到 `03_我的企业Skill成品`。
- **禁止范围**：不引入未授权第三方代码；不放未经测试的模块进成品。
- **完成条件**：`1.0.0` 全模块通过、RELEASE 证据齐全。
- **真实验收**：Release 质检清单 + evidence 落库 `05`。
- **门禁**：通过后入库 `03`；否则回到阶段 8/9 修复。

---

## Release 后的运营（非本 Skill 施工范围，仅约定）
- 版本演进遵循 `08`：Post-release 反馈 → candidate → 独立 Gate → staging → adopt。
- 未来「企业Skill优化器」承接对比反思/Gate；「SkillFactory/企业Skill生成器」承接沉淀——均为独立 Skill，见 `03`「未来演进」，不并进本 Skill（边界 `01`）。

---

## 施工计划自检
- [x] 阶段拆为 10 个，每段有 目标/输入/范围/禁止/完成条件/验收/失败/门禁
- [x] 顺序依赖（骨架→理解→设计→治理→施工→验收→发布→测试→Harness→Release）
- [x] **阶段 1 落地施工前理解门禁骨架 + 状态机跳转测试 + 合同 schema 校验（本次补强新增）**
- [x] 阶段 9 明确不擅动 Harness
- [x] License/来源在阶段 1 与 10 两端体现
- [x] 每阶段「真实验收方式 + 失败处理」齐备
- [x] **阶段 1（建立骨架）已获授权，立即执行；阶段 2+ 需逐步另行授权**

> 设计阶段到此为止。施工计划仅用于后续授权后的执行依据。