# 02｜开源项目能力对照表

> 目录：`02_Skill二次开发区\企业AI项目交付Skill`
> 目的：真实记录从 5 个参考仓库中**实际读取**的关键路径与能力，区分「来自真实源码」与「工程推断」，为设计决策提供来源依据。

---

## 1. 实际读取关键路径（本阶段真实执行）

### 1.1 addyosmani-agent-skills（工程主参考）

已确认结构：根目录含 `skills/ agents/ .claude/commands/ commands/ docs/ evals/ hooks/ references/ scripts/ plugin.json .claude-plugin/ .codex-plugin/ AGENTS.md CLAUDE.md`

`skills/` 实测 26 个子目录，含：`spec-driven-development`、`planning-and-task-breakdown`、`incremental-implementation`、`test-driven-development`、`source-driven-development`、`doubt-driven-development`、`context-engineering`、`frontend-ui-engineering`、`api-and-interface-design`、`browser-testing-with-devtools`、`debugging-and-error-recovery`、`code-review-and-quality`、`code-simplification`、`security-and-hardening`、`performance-optimization`、`git-workflow-and-versioning`、`ci-cd-and-automation`、`deprecation-and-migration`、`documentation-and-adrs`、`observability-and-instrumentation`、`shipping-and-launch`、`using-agent-skills`、`idea-refine`、`interview-me`、`constraint-driven-development`

实测支持文件：`references/definition-of-done.md`、`references/orchestration-patterns.md`、`references/testing-patterns.md`、`references/security-checklist.md`、`.claude/commands/spec|plan|build|test|review|ship.md`、`agents/code-reviewer|security-auditor|test-engineer|web-performance-auditor.md`、`docs/skill-anatomy.md`、`evals/`。

### 1.2 agentskills-standard（开放标准）

实测：`docs/specification.mdx`（规格）、`docs/skill-creation/best-practices.mdx`、`docs/skill-creation/evaluating-skills.mdx`、`docs/skill-creation/optimizing-descriptions.mdx`、`skills-ref/src/skills_ref/validator.py`、`skills-ref/src/skills_ref/parser.py`。

### 1.3 anthropic-knowledge-work-plugins（企业岗位）

实测岗位目录：`finance/ legal/ human-resources/ customer-support/ productivity/ product-management/ sales/ marketing/ operations/`。结构：`<岗位>/.mcp.json + CONNECTORS.md + skills/<skill>/SKILL.md + README.md`。代表 Skill：`finance/skills/reconciliation`、`close-management`、`journal-entry-prep`、`variance-analysis`、`audit-support`；`product-management/skills/feature-spec` 等。

### 1.4 microsoft-skill-recorder（未来工作流采集）

实测：`common/types.ts`（SessionMeta/RecEvent）、`common/events.ts`（事件类型）、`electron/recorder/controller.ts`（状态机）、`session-store.ts`、`collector.ts`、`electron/sensitive/frame-redact.ts`、`ocr.ts`。

### 1.5 microsoft-skillopt（评估/演进参考）

实测：`skillopt/types.py`（Edit/Patch/RolloutResult/FailureSummary）、`skillopt/evaluation/gate.py`（GateResult/evaluate_gate）、`skillopt_sleep/evidence.py`、`rollout.py`（minibatch/contrastive）、`judges.py`（RuleJudge）、`staging.py`（Staging+Adopt）。

### 1.6 openai-skills-reference（只读参考）

实测：`skills/.system/`（OpenAI 自有：imagegen/openai-docs/plugin-creator/skill-creator/skill-installer，**无 LICENSE**）、`skills/.curated/`（39 个第三方 Skill，各含 LICENSE.txt=Apache-2.0）。README 声明 **deprecated**，许可以下发到各 Skill 目录 `LICENSE.txt`。**本阶段仅只读，未复制任何内容。**

---

## 2. 能力对照表（可迁移 → 本项目采纳）

| 开源能力 | 来源仓库 | 是否采纳进本项目 | 采纳方式 | 备注 |
| -------- | -------- | ---------------- | -------- | ---- |
| SKILL.md 标准 frontmatter（name/description/license/compatibility/metadata） | agentskills-standard | ✅ 全采纳 | 主/子 SKILL.md 强制契约 | 唯一协议边界 |
| 渐进式披露（目录→正文→资源，主文件≤500 行） | agentskills-standard | ✅ 全采纳 | 模块+references/scripts 拆分 | 控 Token |
| description 祈使式触发 + should/should-not 触发 eval | agentskills-standard / addyosmani | ✅ 全采纳 | 各模块描述设计与 eval | 触发准确度 |
| Skill=SKILL.md+scripts+references+assets 四件套 | agentskills-standard | ✅ 全采纳 | 每阶段模块 | — |
| Skill 三阶段 eval（structural/trigger/behavioral） | addyosmani | ✅ 采纳为 Skill 自测 | `07` 测试设计 | 防漂移 |
| 反合理化 / Red Flags / Verification 章节 | addyosmani | ✅ 采纳 | 每模块固定章节 | 防跳过 |
| Definition of Done + 每任务 acceptance criteria | addyosmani | ✅ 采纳 | 共享 references/DoD | 防「测过≠能用」 |
| Skill(how)/Persona(who)/Command(when) 三层 | addyosmani | ✅ 部分采纳 | 主 Skill 编排 + 逻辑角色 | 不硬造 Agent |
| slash command 编排（/spec /plan /build …） | addyosmani | ⚠️ 参考 | 映射为模块门禁顺序 | 具体命令按 Harness 而定 |
| 岗位插件 = skills+commands+connectors+mcp | anthropic | ✅ 部分采纳 | 模块按生命周期分组 | 借鉴目录组织 |
| 工具无关抽象（`~~category` 占位符） | anthropic | ✅ 采纳 | 权限/接口设计用类别 | 不绑 SaaS |
| `.mcp.json` 预配置+可替换 | anthropic | ✅ 采纳（协议层） | Harness MCP 映射 | Harness 注入而非写死 |
| append-only 事件流 + 会话元数据（events.jsonl/session.json） | microsoft-skill-recorder | ✅ 采纳为 Evidence 设计 | `06` Evidence JSONL | 审计/回放 |
| 单调 seq + type→payload 类型契约 | microsoft-skill-recorder | ✅ 采纳 | Evidence 事件结构 | — |
| OCR 本地遮罩 / 隐私保护 | microsoft-skill-recorder | ⚠️ 预留 | 未来工作流采集 Skill | 本项目不直接用 |
| Gate 决策（hard/soft/mixed，accept/reject/best） | microsoft-skillopt | ✅ 采纳 | `07` 验收 Gate | Skill 自身版本演进 |
| RuleJudge（结构化 checks，形状 vs 结果） | microsoft-skillopt | ✅ 采纳 | 防「改模板不改行为」 | — |
| Evidence Log（append-only、redact、阶段标签、长度上限） | microsoft-skillopt | ✅ 采纳 | `06` Evidence 合同 | — |
| Staging + Adopt（禁止直接改 live） | microsoft-skillopt | ✅ 采纳 | `08` 升级回滚 | 安全升级 |
| Contrastive Reflect / minibatch | microsoft-skillopt | ⚠️ 预留 | 未来企业Skill优化器 | 本阶段不集成 |
| 单文件超大 SKILL（被否决） | — | ❌ 不采纳 | — | 见 `01` 2.3 |
| 隐藏 Orchestrator Agent（被否决） | addyosmani（实为无） | ❌ 不采纳 | — | 编排落主 Skill/Harness |

---

## 3. 「真实源码」vs「工程推断」分隔

### 3.1 属「从真实源码读取」的结论
- 各仓库目录结构、文件名、SKILL.md frontmatter 字段、`~~category` 模式、events.jsonl 结构、GateResult 字段、RuleJudge checks、staging.py 行为、agentskills validator 硬性约束——均来自实际 Read/LS 输出。

### 3.2 属「工程推断」的结论（须标注）
- 「编排责任落主 Skill/Harness、不新造 Orchestrator」：对 addyosmani 实现方式的归纳与在企业场景的推论。
- 「单 skill_id 单入口对 Harness 最友好」：基于可维护性/权限隔离的工程判断。
- 「主文件 ≤500 行是最佳」：采纳 agentskills 建议值，结合 Token 成本推断。
- 「岗位/角色分离用逻辑角色而非硬性多 Agent」：综合 anthropic 岗位模式与企业成本/运维的推断。
- 各模块数量与分组的最终取舍：基于生命周期完整性 + 可维护性的推断（参考 addyosmani 26 个 Skill 的颗粒度）。

---

## 4. 结论：哪些可直接带进企业，哪些需改造

| 能力 | 企业适用性 | 处理 |
| ---- | ---- | ---- |
| 标准 frontmatter、渐进式披露、DoD、Verification、eval | 高 | 直接采用 |
| 反合理化/Red Flags | 高 | 对团队尤其有用，直接采用 |
| 三层 Skill/Persona/Command | 中-高 | 采用其职责分离思想，命令层随 Harness 落地 |
| append-only Evidence、Gate、Staging+Adopt | 高 | 直接采用为合同 |
| `~~category` 工具抽象 | 高 | 采用 |
| 纯 Markdown 需自建脚本验证 | 中 | 施工期用 scripts 封装，进 CI |
| 需真实用户数据（记录/采集类） | 低（非本 Skill 目标） | 仅预留，不混入 |
| OpenAI .system 内容 | 禁止 | 仅只读，见 `09` |

> 本表与 `09_License与来源边界.md` 互为补充：本表看「能力如何拿」，`09` 看「能不能合法拿、怎么保留声明」。