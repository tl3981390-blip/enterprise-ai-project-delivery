# 01｜企业AI项目交付Skill · 总体架构设计

> 目录：`02_Skill二次开发区\企业AI项目交付Skill`
> 阶段：架构设计与二次开发方案（设计阶段，非施工）
> 英文内部 ID：`enterprise-ai-project-delivery`
> 日期：2026-08-30
> 状态：**Architecture C 已正式采用，Stage 1 已实现，当前持续施工**

---

## 0. 本文件要解决的问题

回答三个问题：

1. 这个 Skill 到底解决什么问题（边界）。
2. 采用什么形态（单 Skill / 主 Skill+子 Skill / 多 Skill+Orchestrator）。
3. 整体架构与模块如何组织，并标明每个设计决策的来源。

---

## 1. Skill 边界定义（做什么 / 不做什么）

### 1.1 它是什么

企业想开发一个新的 **AI 内部产品**时，从「业务问题」开始，穿越 需求 → 架构 → RAG → Agent → 权限 → 开发 → 测试 → 真实验收 → 部署 → 更新 → 回滚，产出一套**完整、可信、可验证**的项目交付过程与证据链的方法论工作流。

目标使用者：企业内部 AI 团队、Applied AI Engineer、FDE、AI Product Engineer、AI 开发团队、企业数字化团队、Agent 开发团队。

### 1.2 它不是什么

| 不是 | 原因 |
| ---- | ---- |
| 写代码 Skill | 它不直接替你把业务逻辑写完，而是约束「先规格、再施工、后验收」。具体编码仍由 Agent 进行，但必须走本 Skill 的门禁。 |
| 生成提示词 Skill | 它不是提示词模板库，是一套工程交付方法。 |
| 万能 Agent | 它不替代 Agent；它指导 Agent / 多 Agent 如何有序、可验证地完成交付。 |
| 巨型提示词 | 它是可拆解、可执行、可验证、可回滚、可治理的方法包。 |

### 1.3 一句话目标（升级版）

> 先确保 AI 做的是用户真正要求的事情，再确保最终结果能够用真实 Evidence 证明已经正确完成。

即：从「AI 说做完了」变成「真实可以证明已完成」，并在此之前先解决「AI 是否理解了用户要求」这个前提。

### 1.4 最高级原则（本次升级新增，优先级高于一切）

> **理解完成之前，禁止施工。**

本 Skill 最重要的能力不是「教 AI 怎么开发」，而是首先解决：AI 在没有真正理解用户目标、当前状态、约束和成功标准以前就开工，随后发生目标漂移、越界修改、错误执行，最后靠「理解错了」「你说得对」事后补救。企业不需要这种「事后道歉型 AI」。因此最高原则：

```text
PRE_EXECUTION_UNDERSTANDING_GATE（施工前理解门禁）：
任何任务进入 Skill 后，禁止直接 WRITE/EDIT/DELETE/EXECUTE/DEPLOY/MIGRATE/INSTALL/ALTER；
必须首先进入 UNDERSTANDING 状态，理解完成、合同锁定、计划对账通过后才逐步开放写权限。
```

### 1.5 核心原则（贯穿所有模块）

- **理解优先 > 快速开工**：先 Prove 理解，再动工（本节 1.4）。
- **目标一致 > 模型的“最佳实践”**：不做目标外的“顺手优化”。
- **事前阻断 > 事后道歉**：把错误尽量阻断在 UNDERSTANDING 与 PLAN 阶段；道歉不是验收机制。
- **用户真实意图 > AI 自行推断**：重大范围/权限/目标不能仅凭 `AI_INFERRED` 自动升级为合同事实。
- **做对 > 仅仅做完**：目标不漂移、不越权、不假验收，才算完成。
- **Outcome > Process**：最终看企业真实目标是否完成，不数 Agent、不数报告、不数脚本。
- **先规格后编码**（SDD）、**关键逻辑先测试**（TDD 判断式应用）。
- **Evidence 优先**：一切结论必须有真实证据，模型总结文字不算证据。
- **防假验收**：只有真实执行结果能驱动 PASS。
- **权限与合规内建**：Skill 默认不拥有管理员 / 生产库 / root 权限。
- **来源可追踪**：每个设计决策标明来自哪个开源项目或我的方法论。

---

## 2. 核心架构选择（方案对比与结论）

### 2.1 候选方案

| 方案 | 形态 | 说明 |
| ---- | ---- | ---- |
| A | 一个超级大的 SKILL.md | 全部流程写进单个文件 |
| B | 主 Skill + 子 Skill / 模块 | 一个主 Skill 负责编排与总入口，各阶段作为子 Skill（模块） |
| C | 多独立 Skill + Orchestrator | 多个互相独立的 Skill，另设一个编排 Skill/Agent |

### 2.2 多维度对比

| 维度 | A 单一大文件 | B 主+子 | C 多+Orchestrator |
| ---- | ---- | ---- | ---- |
| 可维护性 | 差，改一处牵一发动全身 | 好，模块独立 | 好，但多套注册管理 |
| Token 消耗 | 高，一次性全加载 | 低，渐进式披露按需加载 | 低，但编排开销 |
| Agent 理解 | 差，上下文过载 | 好，职责清晰 | 中，需理解编排 |
| 触发准确度 | 差，一入口难精准路由 | 好，主描述总触发+子描述阶段触发 | 好，但需额外路由 |
| 版本管理 | 差，单一版本难拆分 | 好，按模块可独立版本 | 好但复杂 |
| Harness 接入 | 简单但笨重 | 最佳，单 skill_id 单版本注一入口 | 复杂，多 skill 注册 |
| 企业部署/权限隔离 | 差 | 好，按模块隔离 | 好 |
| 可测试性 | 差 | 好，模块级 eval | 好 |
| Skill 单独复用 | 不可 | 中，模块可复用 | 高 |
| 跨模型兼容 | 中 | 高（符合开放标准） | 高 |

### 2.3 结论

**采用 Architecture C：Independent Enterprise Skill + Selective Upstream Reuse。**

内部仍使用“主入口 + 阶段模块”的渐进披露结构，但这只是 Skill 内部组织，不是对 Spec Kit 的 Fork 或运行时依赖。唯一权威状态机、权限绑定与 Evidence Gate 均由本 Skill 掌握。

理由（来源映射见第 4 节）：

1. **标准兼容优先**（来源：agentskills-standard）：开放标准用渐进式披露（目录→正文→资源），主 SKILL.md 建议 ≤500 行，其余放 references/scripts/assets。方案 B 天然契合。
2. **工程参考验证**（来源：addyosmani-agent-skills）：该仓库实际采用「Skill(how)+Persona(who)+Command(when)」三层，由 meta-skill `using-agent-skills` 做发现与编排，而非隐藏 Orchestrator。这与「主 Skill 编排 + 模块执行」一致。
3. **企业岗位模式**（来源：anthropic-knowledge-work-plugins）：一个岗位 = skills + commands + connectors + mcp，skills 内部组织。方案 B 与之一致。
4. **避免过度 Agent 化**：方案 C 需要额外 Orchestrator Agent，增加部署与权限复杂度。我选择**让编排责任落在主 Skill 与 Harness 上**，而不是再造一个 AI 编排 Agent（来源：addyosmani 明确「编排责任交给用户/slash command」的工程推断；以及我的「多 Agent 只在真正需要角色分离时使用」原则）。
5. **Harness 友好**：一个 `skill_id`、一个入口、一个版本，Harness 只需注册一次，权限隔离在子模块边界。

> 说明：方案 B 的「主+内部模块」在物理组织上 = 1 个公开 `SKILL.md` + N 个阶段模块（每模块一套 `MODULE.md/references/scripts/tests`），全部继承同一 `skill_id` 与 `semver` 版本。Harness 和 `/` 菜单只面对一个入口，内部由主 Skill 按事件读取模块。

---

## 3. 整体架构分层

```text
企业AI项目交付Skill (enterprise-ai-project-delivery)
│  = 1 主 SKILL.md（编排 + 总入口）
│  + 阶段模块（每个模块 = MODULE.md + references/ + scripts/ + tests/）
│  + 共享 references/（统一检查清单）
│  + 共享 scripts/（通用工具，如 evidence 打包、license 扫描、skill 自检）
│  + tests/（Skill 本身的测试与 eval）
│
├─ S0  总控与阶段门禁（编排：施工前理解门禁 / 状态机 / 任务理解合同 / 计划-合同对账 / DRIFT_CHECK / 权限阶段控制 / 停止条件 / 人工介入）
│
├─ 阶段 0'  理解与锁定（最高门禁，位于一切施工之前）
│  [UNDERSTANDING] → [任务理解合同] → [施工前理解门禁] → [READY_TO_PLAN]
│
├─ 阶段 A  理解与审计
│  [01 项目理解与目标] [02 当前状态审计]
│
├─ 阶段 B  需求与规格
│  [03 需求与范围] [04 SDD规格] [05 TDD与测试策略] [06 架构设计]
│
├─ 阶段 C  治理设计
│  [07 RAG设计] [08 Agent设计] [09 MCP与工具权限网关] [10 企业治理与合规]
│
├─ 阶段 D  施工与质量
│  [11 施工管理与增量实现] [12 失败处理与恢复]
│
├─ 阶段 E  验收
│  [13 浏览器真实验收] [14 多角色验收] [15 Evidence与防假验收]
│
├─ 阶段 F  发布与运营
│  [16 部署] [17 License与合规] [18 升级与回滚]
│
└─ 阶段 G  收尾与沉淀
   [19 最终交付与经验沉淀]
```

### 3.1 分层职责

- **主 SKILL（S0 及总控）**：总描述（触发）、**施工前理解门禁（UNDERSTANDING→合同→Gate）**、阶段推进与门禁、DRIFT_CHECK、停止条件、人工介入条件、输出合同、Evidence 汇总。
- **阶段 0' 理解与锁定**：任何任务先进入 UNDERSTANDING，禁止直接写改；回答施工前八问 → 生成《任务理解合同》→ 施工前理解门禁 → 状态机 `UNDERSTANDING_BLOCKED/BLOCKED/UNDERSTANDING_COMPLETE/READY_TO_PLAN`（详见 `00_总控\references\状态与权限矩阵.md`）。
- **阶段模块**：单一生命周期阶段的具体工作流 + 完成判定（模块级 Verification）。所有模块受 S0 的理解合同与 DRIFT_CHECK 约束。
- **共享 references**：Definition of Done、验收检查清单、安全治理清单、证据清单、任务理解合同模板、施工前理解门禁、目标漂移检查清单、状态与权限矩阵 —— 被多个模块引用，避免重复（来源：addyosmani `references/` 模式）。
- **共享 scripts**：通用可执行工具（如 `validate-skill`、`collect-evidence`、`check_understanding_gate`、`check_plan_alignment`），要求 stdout 结构化、`--help`、破坏性操作 `--dry-run`（来源：agentskills best-practices）。
- **tests**：Skill 自身结构与行为测试、eval 用例（来源：addyosmani evals 三阶段 + agentskills evaluating）。

---

## 4. 关键设计决策 → 来源映射

| # | 决策 | 来源 |
| -- | ---- | ---- |
| 1 | 采用 Architecture C；内部为主入口+阶段模块 | 上游 Rebase 评审；agentskills-standard 渐进披露 |
| 2 | SKILL.md 使用标准 frontmatter：name/description/license/compatibility/metadata | agentskills-standard（validator 字段集） |
| 3 | description 用祈使式「做什么+何时用」承担触发 | agentskills-standard（optimizing-descriptions） |
| 4 | 公开入口 = 根 SKILL.md；内部模块 = MODULE.md + references + scripts + tests | Codex 真实发现行为 + 渐进式披露 |
| 5 | 共享 references 避免重复（DoD、checklist） | addyosmani references/（definition-of-done.md 等） |
| 6 | 每模块含「反合理化、Red Flags、Verification」 | addyosmani SKILL.md 章节约定 |
| 7 | 编排责任落主 Skill/Harness，不新造 Orchestrator Agent | addyosmani 工程推断；我的「多 Agent 适度」原则 |
| 8 | 工具无关抽象（工具类别占位符，而非绑定具体 SaaS） | anthropic CONNECTORS.md（`~~category` 模式） |
| 9 | Evidence 用 append-only 结构化日志 + 阶段标签 | microsoft-skill-recorder（events.jsonl/session.json）；microsoft-skillopt（evidence.jsonl） |
| 10 | 验收用硬性 Gate 决策（PASS/FAIL，唯一来源真实证据） | microsoft-skillopt（evaluate_gate）；addyosmani（Verification 需证据） |
| 11 | Skill 自身需 eval：结构/触发/行为三阶段 | addyosmani（evals structural/trigger/behavioral）；agentskills（evaluating-skills） |
| 12 | 自修改需 Staging+Adopt，禁止自宣布成功 | microsoft-skillopt（staging.py） |
| 13 | 阶段性推进用 plan-validate-execute，破坏性操作 prescriptive | agentskills best-practices（validation loops / plan-validate-execute） |
| 14 | 岗位/角色分离，而非硬性多 Agent | anthropic（岗位分组）；我的四角色验收原则 |
| 15 | 一切遵循开放标准，避免绑定单一模型 | 企业目标；agentskills-standard；opennAI 仅作只读参考 |

---

## 5. 本 Skill 与周边体系的关系

```text
Enterprise Skill（本项目，独立开发）
        ↓
Skill Adapter（04_Harness接入适配，未来）
        ↓
Permission Gateway（Tool/MCP 权限映射，未来）
        ↓
Agent Harness（加载/调用/权限/Runtime/日志/Evidence）
        ↓
Agent（可多角色）
        ↓
Model / MCP / Tool
```

- Skill 本体与 Harness **解耦**：Skill 只声明需要什么（required_tools / allowed_tools / required_mcp / permissions），具体注入由 Harness/Adapter 完成（详见 `05`）。
- 本 Skill 不依赖某一个模型，介于开放 Skill 规范之上（详见 `09`）。

---

## 6. 与公司既有方法论的关系

本 Skill **不是** GitHub 项目的复制品。它把我在真实 AI 项目里沉淀的方法论（目标驱动、状态审计、防幻觉/防越权/防旧版本/防假引用、四角色验收、证据链、失败恢复等）抽象为标准模块，并借用开源项目已验证的工程组织方式承载。能力提炼详见 `03`、来源对照详见 `02`。

---

## 7. 本阶段交付物边界

- 本文件及 `02`—`10` 完成设计；**2026-08-30 已完成施工前架构补强**（最高原则、状态机、任务理解合同、DRIFT_CHECK、计划-合同对账），见本节 1.4/1.5、第 3 节及各文档相应章节。
- 补强通过后，**正式施工阶段 1（建立 Skill 骨架）已获授权执行**，见 `10_正式施工计划.md`。
- 阶段 1 仅建立 Skill 自身工程骨架（主 SKILL + 目录 + references + scripts + schema + tests + LICENSE/NOTICE + CHANGELOG + README）；**禁止**实现正式业务项目、连接生产系统、修改 Harness。
- 用户已授权从 Stage 2A 连续施工到 Release；仍须逐阶段 Gate、测试、Evidence 和提交。

---

来源说明：本文件结论主要基于对 5 个仓库的真实源码/Skill 文件读取与工程推断，具体读取路径见 `02_开源项目能力对照表.md` 与最终汇报「实际读取关键文件」。
