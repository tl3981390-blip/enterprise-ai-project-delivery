# 08｜版本更新与回滚设计

> 目的：定义 Skill 自身（以及它交付的产物）的版本演进与回滚机制。核心约束：**Skill/系统不能自己改完就宣布成功；必须走独立验证 + Staging + Adopt；升级失败不破坏当前版本。**

依据：microsoft-skillopt（staging.py、GateResult）、我的能力 `18/22`、semver 工程实践（工程推断）。

---

## 1. 版本体系（semver 语义化）

### 1.1 版本号
格式 `MAJOR.MINOR.PATCH`：
- **MAJOR**：破坏性变更（改 frontmatter 契约、移除了模块、改了模块门禁顺序、接口不兼容）。
- **MINOR**：新增模块/能力、向后兼容增强。
- **PATCH**：修 bug、文字/描述优化、脚本修复，不改契约。

> 版本是**本 Skill 自身**的版本，与客户项目内部版本无关；二者分开管理。

### 1.2 版本状态标签
| 标签 | 含义 | 可否用于生产 |
| ---- | ---- | -------------- |
| `-dev` | 施工/开发中 | 否 |
| `-candidate`（candidate） | 已完成三阶段 eval，待独立验收 | 有条件 |
| `-stable`（previous stable） | 当前稳定版 | 是 |
| `-deprecated` | 已废弃，仅警告 | 否（只读提示迁移） |
| `Rollback` | 指回上一个 stable | 应急 |

---

## 2. 变更流程（禁止自通过）

```text
当前版本 candidate
   ↓ 修改 SKILL/模块/描述/脚本
新 candidate
   ↓ 三阶段 eval（结构/触发/行为）+ 专项回归   ← 独立于"修改者自评"
   ↓
Gate 决策（借鉴 skillopt evaluate_gate）：
    候选分 > 当前 stable 分 → 可进入 staged
   ↓
写 Staging（不直接覆盖 live）
   ↓
人工/独立验证 approve
   ↓
Adopt 复制覆盖 current stable + 备份旧版
   ↓
写 CHANGELOG + 兼容性 + migration 说明
```

### 关键规则
1. **禁止**修改者自己改完后直接宣布成功。必须有独立`Validation Gate`（依据 skillopt gate + 我的能力）。
2. **Staging 先行**：改动先写 staging 目录与人类可读报告，再由显式 adopt 覆盖 live，并备份（依据 skillopt staging.py）。
3. **升级失败不破坏当前版本**：adopt 前保留 previous stable，可一键 Rollback。

---

## 3. 兼容性 / Migration / Deprecated

- **Backward Compatibility**：MINOR/PATCH 不得破坏已通过验收的项目工作流；MAJOR 需提供迁移说明。
- **Migration**：MAJOR 变更必须附 迁移指南（如模块重命名、证据 schema 升级），并跑迁移测试。
- **Deprecated**：被替代模块打 `-deprecated`，仍可回退使用，但触发时提示"请迁移到 X 模块"。
- **Previous Stable**：始终保留上一 stable 的可回退副本。

---

## 4. CHANGELOG / Release Notes

每个版本必须产出：

```jsonc
{
  "version":    "1.1.0",
  "date":       "2026-xx-xx",
  "type":       "minor",
  "summary":    "…",
  "changed":    ["…"],
  "added":      ["…"],
  "removed":    ["…"],
  "deprecated": ["…"],
  "security":   ["…"],
  "migration":  ["…"],   // MAJOR 必填
  "evidence_refs":["…"]  // 该版本通过的三阶段 eval 结果引用
}
```

---

## 5. 回滚机制

| 场景 | 动作 |
| ---- | ---- |
| Skill 自身（adopt 后发现问题） | 用备份切回 previous stable，记录回滚原因+证据 |
| Skill 交付的项目（客户发生了回归） | 项目侧执行其在 `18` 定义的回滚演练/回滚包（不是 Skill 回滚） |
| 升级中途失败 | 停在 staged，live 保持原版，无破坏 |

> 回滚本身也要有证据（真实执行回滚并验证恢复），不能"写了脚本=能回滚"（我的能力 `22`）。

---

## 6. 反自循环 / 停止条件（升级也受约束）

Skill 优化、修改到什么程度停？
- max_loop：单次优化最多 N 轮（默认 3，可配置）。
- blocking：出现无法自动修复的失败 → 停下来，交人工。
- human_required：涉及契约/权限/行为变更 → 必须人工 approves。
- RuleJudge 防形状改进：只改格式不改行为不得算"改进"（依据 `07`）。

---

## 7. 谁负责独立验证（角色）
- Skill 修改者（施工 Agent）不可兼任最终验收。
- 独立验收 = 三阶段 eval 结果 + 四角色/治理视角 + 人工 approve（对应 `14` 四角色与 Skill 自身 `07`）。
- 企业内可配置固定评审角色。

---

## 8. 设计完成度自检
- [x] semver 版本号、状态标签、previous stable/candidate/deprecated/rollback
- [x] Staging+Adopt+备份
- [x] 独立 Validation Gate 防自通过
- [x] CHANGELOG/Release Notes schema
- [x] Migration/BackCompat/Deprecated
- [x] 回滚机制 + 回滚证据
- [x] 停止条件
- [ ] semver 校验脚本、staging 目录约定、CHANGELOG 模板 → 施工期 `10`
- [ ] 与 `07` 回归、`17` License 的联动落地